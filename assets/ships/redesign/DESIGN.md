# Harbor geometry

This payload now contains 27 harbor, workshop, item and decorative designs. Ship redesign entries have been removed: all current ships load their own HMF geometry from `../final/` onto native Valheim components.

The harbor designs still use retained source hierarchies and some original visible assets. `designs.json` lists coverage; `../../../design/DECOR-REMODEL-PLAN.md` describes the remaining replacement work. `tests/BoatyardModels/check.py` checks every retained target and mesh against the reduced bundle.
